import ReactDOM from "react-dom/client";
import DataFetching from './ten';
const rootElement=document.getElementById('root')
const rootRef=ReactDOM.createRoot(rootElement)
rootRef.render(<DataFetching />)